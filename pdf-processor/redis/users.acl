# Redis ACL Configuration - LOCAL DEV (No Auth)
# For production, use proper ACL with environment-substituted passwords

# Enable default user with no password (local dev only)
user default on nopass ~* &* +@all